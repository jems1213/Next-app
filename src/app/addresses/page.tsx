export default function AddressesPage() {
  return (
    <section style={{ padding: "28px 20px" }}>
      <div style={{ maxWidth: "var(--container-max)", margin: "0 auto" }}>
        <h1 style={{ fontSize: "var(--type-h2)" }}>Addresses</h1>
        <p className="text-muted">Manage your shipping and billing addresses here.</p>
      </div>
    </section>
  );
}
